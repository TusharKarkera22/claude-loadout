---
description: Re-run the sanitize scan against an existing profile bundle and report secret/path leaks.
argument-hint: "<bundle-dir>"
---

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/cli.js" sanitize $ARGUMENTS
```
