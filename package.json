{
  "name": "claude-profiles",
  "version": "0.1.0-pre",
  "description": "Share your Claude Code setup like dotfiles. Bundle and install profiles of CLAUDE.md, skills, slash commands, and sub-agents.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.10.0"
  },
  "bin": {
    "claude-profiles": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo \"v0.1: tests not yet wired\" && exit 0"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "simple-git": "^3.27.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "keywords": [
    "claude-code",
    "claude-code-plugin",
    "claude-code-profile",
    "dotfiles",
    "ai-coding"
  ]
}
