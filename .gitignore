node_modules/
# dist/ is local-only — typecheck output, not the runtime artifact.
# The actual runtime artifact slash commands invoke is bin/cli.cjs
# (a single self-contained esbuild bundle). CI guards against bin/ drift.
dist/
*.log
.DS_Store
.env
.env.local
.claude-loadout/
.claude-profiles/
my-loadout/
my-profile/
.cache/
coverage/
.vscode/
.idea/
*.tsbuildinfo
